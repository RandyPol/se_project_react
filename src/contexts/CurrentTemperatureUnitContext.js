import React from 'react';

/** Create a context for the value of the weather in the prefer temperature unit. */

const CurrentTemperatureUnitContext = React.createContext();

export default CurrentTemperatureUnitContext;